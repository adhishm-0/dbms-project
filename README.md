AI Career Navigator — Minimal Static Demo

This is a minimal static website scaffold created as a fresh copy of the AI Career Navigator frontend for quick demos.

Files:
- index.html — static landing page
- styles.css — simple styles

To push this to a new GitHub repo:

```bash
cd AI-Career-Navigator-fresh
git init
git add .
git commit -m "Initial scaffold: minimal static demo"
# create a GitHub repo and add remote, then:
# git remote add origin https://github.com/<your-org>/<repo>.git
# git push -u origin main
```
